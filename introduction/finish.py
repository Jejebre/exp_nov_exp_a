# In this window is the program you want to send.

#Try to uncomment the right command


@onevent
def buttons():
    global motor_left_target, motor_right_target 	#Write this to modify global variables
    if button_forward: # Check if we press the forward button
        #motor_left_target = 0 					# If so, go forward...
        #motor_right_target = 0
        #motor_left_target = 100 					# If so, go forward...
        #motor_right_target = 100
        #motor_left_target = -100 					# If so, go forward...
        #motor_right_target = -100